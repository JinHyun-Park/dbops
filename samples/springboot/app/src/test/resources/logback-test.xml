<?xml version="1.0" encoding="UTF-8"?>
<!--
  Test-only logging config. Takes precedence over logback-spring.xml on the test
  classpath, so tests do NOT write to the production file path
  (/var/log/todoapp/app.log), which is not writable in a local/CI build.
  Console only; the JSON file appender is exercised on the EC2 host at runtime.
-->
<configuration>
  <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
    <encoder>
      <pattern>%d{ISO8601} [%level] %logger{0} - %msg%n</pattern>
    </encoder>
  </appender>

  <root level="INFO">
    <appender-ref ref="CONSOLE"/>
  </root>
</configuration>

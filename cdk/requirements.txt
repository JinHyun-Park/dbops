aws-cdk-lib>=2.170.0
constructs>=10.0.0
aws-cdk.aws-bedrock-agentcore-alpha
# Synth-time security lint (AWS Solutions ruleset), gated behind CDK_NAG=1 in
# app.py — opt-in, never blocks a normal deploy. Pinned to the 2.x line: cdk-nag
# 3.x needs aws-cdk-lib>=2.260 whose cloud-assembly schema (54) outruns the
# pinned CDK CLI, breaking synth.
cdk-nag>=2.28,<3

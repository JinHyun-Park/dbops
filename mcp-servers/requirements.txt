# Dependencies bundled into the MCP-servers Lambda asset.
# boto3/botocore ship in the Lambda runtime. Extra dependencies:
# - pymongo for DocumentDB Mongo-protocol write tools (set_docdb_profiler,
#   create_docdb_index) in the operations server
# - redis for ElastiCache live deep-read (elasticache_live_read)
# - pymemcache for Memcached inspection in ElastiCache live deep-read
# The other MCP servers share the same asset and get these too (harmless, unused).
# pymongo pin matches docdb_mongo_collector.
pymongo>=4.17.0
redis>=8.0.1
pymemcache>=4
# pg8000: pure-Python PG driver (no C build) for prewarm_reader's direct
# connection to a reader INSTANCE endpoint (RDS Data API can't target one instance).
pg8000>=1.31
